#include <stdio.h>
#include "main.h"

/**
 *main - Entry point of the code
 *@argc: argument count
 *@argv: String arguments at function call.
 *
 * Description: Function that prints its name, followed by new line
 * Return: Always 0 (success)
 */


int main(int argc, char *argv[])
{
	if (argc > 0)
		printf("%s\n", argv[00]);
	return (0);
}
