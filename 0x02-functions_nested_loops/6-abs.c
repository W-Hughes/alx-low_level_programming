#include "main.h"

/**
 *_abs - computes the absolute value of an integer values
 *@n: The integer to comptute the absulute value of
 *
 *Return: the absolute value of the integer
 */

int _abs(int n)
{
	return ((n >= 0) ? n : -n);
}
