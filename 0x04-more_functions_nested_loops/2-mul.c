#include "main.h"

/**
 *mul - Multiplies two integers.
 *@a: an integer that's a factor.
 *@b: an integer to be multiplied(a factor).
 *
 * Return: the product of the factors.
 */

int mul(int a, int b)
{
	return (a * b);
}
